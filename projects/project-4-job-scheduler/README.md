# Project 4 — Job Scheduler

Purpose

- Implement a lightweight job scheduler or demonstrate existing schedulers (cron, APScheduler) with metrics and retry behavior.

Primary tech

- Python (`APScheduler`), cron, Docker

Run / Demo

- Provide a small service that schedules and runs jobs, exposing metrics for job success/failure.

What to include

- example `scheduler.py`
- instructions to run and test job retries
- metrics export (Prometheus client)
